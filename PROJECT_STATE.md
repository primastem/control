# PrimaSTEM — Robot BLE Control — состояние и план работ

> Хендофф для продолжения в Claude Code. Дата: 2026-06-03. Команда: PrimaSTEM (Андрей — RU, Мика — FR/EN).

## Что это
Веб-приложение управления роботом PrimaSTEM по Bluetooth Low Energy через **Web Bluetooth API**. Один самодостаточный файл `primastem-robot-control.html` (чистый HTML+CSS+JS, без сборки и зависимостей).

## Структура проекта
```
BLE/
├── primastem-robot-control.html   # САМО приложение (единственный файл с кодом)
├── ble.md                          # исходник протокола (рус)
├── docs/
│   ├── ble-protocol.ru.md          # финальная документация протокола (рус)
│   ├── ble-protocol.en.md          # финальная документация протокола (англ)
│   └── primastem_ble_robot.jpg     # схема структуры BLE
├── primastem_ble_robot.jpg         # копия схемы
└── backups/<timestamp>/            # снимки рабочих состояний (ручной откат)
```

## Протокол (кратко) — полный текст в docs/ble-protocol.*.md
- Сервис `0x0100`, характеристики: `0x0101` команды (R/W), `0x0102` статус (notify: 0x00 free / 0x01 busy).
- **Реальные UUID (кастомная база, подтверждено макросом прошивки UUID_CREATE):**
  формула `bd9e1632-<a:04x>-4d63-ad5f-27f115379843`
  - сервис  = `bd9e1632-0100-4d63-ad5f-27f115379843`
  - команда = `bd9e1632-0101-4d63-ad5f-27f115379843`
  - статус  = `bd9e1632-0102-4d63-ad5f-27f115379843`
  Короткие 0x01xx — только ярлыки, для подключения НЕ годятся.
- Пакет команды: 9 байт = `type`(uint8) + `main`(uint32) + `second`(uint32), **little-endian**.
- Команды (enum `ble_cmd_type_t`): log=0x01, move=0x02, led=0x03, sound=0x04, conf=0x05, lvl=0x06(не описана), name=0xAA, break=0xFF.
- move: main = направление 'f'/'b'/'r'/'l' (код символа в правом байте), second = мм (перемещение) или градусы (поворот).
- led: main = 'l'/'r'/'b' (диод), second = 'r'/'g'/'b'/'w' (цвет). Блокирующая.
- sound: main = имя аудиофайла (формат XXX.mp3). Неблокирующая.
- conf: main = 'l' (калибровка длины) / 'i' (калибровка IMU), second = параметр.
- break(0xFF): СТОП, прерывает текущую команду.
- name(0xAA): main = 4 символа → имя ROBOTxxxx, робот перезагружается.
- Пример: вперёд 90 мм = `02 66 00 00 00 5A 00 00 00`.
- Робот: имя ROBOTZCTN, MAC 50:78:7D:0B:0A:3A (тестовый экземпляр).

## Реализовано сейчас
- Подключение (filter namePrefix "ROBOT", optionalServices = service UUID).
- Движение: вперёд/назад/влево/вправо + СТОП (cmd_break).
- Поля «Дистанция» (дефолт 150 мм, для вперёд/назад) и «Угол» (дефолт 90°, для поворотов) — редактируемые.
- Подписка на статус 0x0102: индикатор free/busy, блокировка кнопок пока busy.
- Языки EN (по умолчанию) / FR / RU, переключатель, выбор в localStorage (ключ `ps_lang`).
- Раздел «Настройки BLE»: 3 поля UUID (заполнены реальными значениями, редактируемые).
- Фирменная тёмная палитра docs.primastem.com: зелёный #68B833/#5AA02C/#4D8825, фон #121512, карточки #1c1f1a/#2a2d27, границы white@10%, текст белый, muted #A1A39F. Текст на зелёных кнопках — белый (--on-accent:#fff).
- Проверка совместимости: предупреждение если нет Web Bluetooth или открыто из file:// (нужен https/localhost).

## План работ (приоритет — СЕЙЧАС) — ✅ ВЫПОЛНЕНО (2026-06-03)
Все 7 пунктов реализованы в `primastem-robot-control.html`. Проверено: страница грузится без JS-ошибок, сборка всех пакетов сверена с эталоном протокола через Chrome DevTools.
1. ✅ **LED (cmd_led 0x03):** segmented-выбор диода (л/п/оба) + 3 цвета (кр/зел/гол), кнопка «Вспышка». Блокирующая — ждёт free, под watchdog. Поведение робота: диоды на секунду меняют цвет, затем возвращаются к белому (дефолт). **Косяки железа учтены:** левый/правый диод физически перепутаны → в UI кнопка «Левый» шлёт `'r'`, «Правый» шлёт `'l'` (своп `data-v`). Белый цвет убран из выбора — робот сам к нему возвращается. Дефолт цвета — красный.
2. ✅ **Звук (cmd_sound 0x04):** поле «Код звука x000–x998 (напр. x005)» + «Играть». **БЛОКИРУЮЩАЯ** (прошивка шлёт busy→free; старая дока ошибочно говорила «неблокирующая»). `send(...,true)` + watchdog. **ПОДТВЕРЖДЕНО ИСХОДНИКОМ ПРОШИВКИ:** main = **число 0…998**, робот строит имя `sprintf("/spiffs/x%03d.mp3", main)` — префикс жёстко `x` + 3 цифры. `x005`→main=5→`x005.mp3` (звук «Hi!» подтверждён). `playSound`→`soundIndex()` берёт цифры из имени (или просто число), отбрасывает ≥999. `x005`→пакет `04 05 00 00 00 00 00 00 00`.
   - **Ограничения по прошивке (учтены в UI):** доступны только `x000`–`x998`. `main≥999` → прошивка пишет `Too long number audio file`, тишина → `soundIndex` режет. `n###`, `cfor`, `sqrt` и пр. **недоступны** (префикс зашит `x`); ввод `n007` фактически сыграет `x007`. soundHint предупреждает.
   - Доки протокола (`docs/ble-protocol.ru.md`/`.en.md`) исправлены: cmd_sound теперь «Да/Yes» (блокирующая), описание + листинг прошивки добавлены.
   - История: ASCII-упаковку (прямой/реверс, main/second, регистр) перебрали — тихо; автоперебор (временная кнопка, удалена) дал «main=число» → затем программист прислал исходник, подтвердивший формат.
3. ✅ **Клавиши-стрелки:** ↑↓←→ = движение, пробел = СТОП. Игнор когда фокус в input; `e.repeat`-флуд и busy блокируются.
4. ✅ **Пресеты:** чипы 50/100/150 мм и 45/90/180°, подсветка активного, пишет в input + триггерит сохранение.
5. ✅ **Запоминание + переподключение:** localStorage `ps_distance`/`ps_angle`/`ps_lang`/`ps_robot`. Кнопка «Последний · <имя>» (фильтр по точному имени, меньше выбора; полный автоконнект в Web BT невозможен без жеста).
6. ✅ **Сырой hex-отправитель:** поля type/main/second (dec или 0x.., LE-сборка) ИЛИ строка «9 байт hex» (отправка как есть, перекрывает поля). Для теста cmd_lvl 0x06. Неблокирующая.
7. ✅ **Watchdog:** `WATCHDOG_MS=15000`. Армируется при отправке блокирующей команды и на busy-notify; снимается на free. По таймауту — разблокировка кнопок + предупреждение в логе. Покрывает и случай отсутствующего stateChar.

### Новое в реализации (сверх п.40-49)
- Команды добавлены в `CMD`: led/sound/conf/lvl/name (полный enum).
- `send(type,main,second,blocking)` — флаг blocking: оптимистичная блокировка + watchdog. move/led — blocking; sound/break/raw — нет.
- `setControls()` теперь гасит и LED/Sound/Raw-кнопки, не только pad.
- Сегментированные контролы (`.seg`), пресет-чипы (`.chip`), два `<details>` (UUID + Raw) в одной карточке.

## Будущие «хотелки» (НЕ сейчас, по решению команды)
- **Конструктор программ:** цепочка команд + «Запуск» с ожиданием free между блокирующими. Сильно в духе продукта, но продукт «без экрана» — позиционировать как демо/инструмент, не замену токенам.
- **Распространение:** разместить онлайн по https (поддомен типа control.primastem.com или GitHub Pages) — без этого не запустить с телефона; iOS Safari не поддерживает Web Bluetooth (обход: браузер Bluefy или нативная обёртка Capacitor).
- **PWA:** manifest + service worker, установка «на экран», офлайн.
- **Калибровка (cmd_conf 0x05)** и **переименование робота (cmd_name 0xAA)** — отложены.

## Открытые вопросы
- `cmd_lvl` (0x06): назначение/параметры неизвестны — проверить «сырым» отправителем (п.6) опытным путём, затем дописать в docs.
- Список аудиофайлов для звука — взять/подтвердить (нужно для п.2).

## Как запускать (важно)
Web Bluetooth НЕ работает из file://. Нужен https или localhost:
```
cd C:\Users\andrei\Documents\Claude\Projects\BLE
python -m http.server 8000
# открыть http://localhost:8000/primastem-robot-control.html в Chrome/Edge
```
На телефоне (Android) — разместить по https. iOS — только через браузер Bluefy.

## Заметки по реализации
- Сборка пакета: `DataView`, setUint8(0,type), setUint32(1,main,true), setUint32(5,second,true) — LE, проверено против примера протокола.
- Запись: `writeValueWithResponse` (характеристика R/W), fallback `writeValue`.
- i18n: словарь I18N {en,fr,ru}, элементы помечены `data-i18n`, setLang() обновляет тексты + классы кнопок.
- Все цвета — через CSS-переменные в :root, легко менять.
